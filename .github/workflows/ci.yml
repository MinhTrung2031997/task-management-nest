name: CI pipeline

run-name: Build CI by ${{ github.actor }}

on:
  push:
    branches:
      - 'develop'
      - 'release'
      - 'main'
  pull_request:
    branches:
      - 'develop'
      - 'release'
      - 'main'
      - 'base/*'

jobs:
  build:
    name: Build & Test

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18.x
          registry-url: https://npm.pkg.github.com

      - name: Install dependencies
        run: yarn install

      - name: Build-source-code
        run: yarn build

      - name: Linting
        id: execute-lint
        if: ${{ always() }}
        run: yarn lint

      - name: Testing
        if: ${{ always() }}
        run: yarn test:cov
